import { memberSchema } from "./member";
import { trackSchema } from "./track";
import { liveScheduleSchema } from "./liveSchedule";

export const schemaTypes = [memberSchema, trackSchema, liveScheduleSchema];
